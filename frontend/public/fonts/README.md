# Offline fonts (IBM Plex Sans Arabic)

Self-hosted copies live in `src/fonts/ibm-plex-sans-arabic/` and are loaded via `next/font/local` in `src/app/[locale]/layout.tsx`.

No Google Fonts network call is required at build or runtime.

Weights: 400, 500, 600, 700 (Arabic + Latin subsets).
